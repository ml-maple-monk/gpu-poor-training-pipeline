# Heavy training base image for Python 3.12 / CUDA 12.8.
# Keep this separate from requirements.train.txt so the lean image can stay
# small while the remote training base carries the full CUDA-serving stack.

datasets==3.6.0
jsonlines==4.0.0
numpy>=2,<3
tqdm>=4.66,<5
huggingface_hub>=0.24.0
transformers==4.57.1
click>=8,<9
mlflow==2.17.2
boto3>=1.34,<2
psutil==5.9.8
nvidia-ml-py==12.560.30

bitsandbytes==0.49.2
torchao==0.17.0
torchtune==0.6.1
vllm==0.11.0
llamafactory==0.9.4
deepspeed==0.16.9
